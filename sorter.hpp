#ifndef SORTER_H
#define SORTER_H

#include <string>

class Sorter {
  private:
    

  public:
    Sorter();

    void appIdToName(std::vector<int> ids);
    // std::vector<int> appIDs();
};


#endif